// const createElements = (arr) => {
//     // console.log(arr);
//     const htmlElements = arr.map((element) => `<span class="btn">${element}</span>`)
//     console.log(htmlElements.join(" ")); // convert the array to string
// }


const synonyms = ['Hello', 'Hi', 'Hola'];
createElements(synonyms)